#include <stdio.h>

int main() {
 int numbers;

 printf("enter an intiger: ");
 scanf("%d",&num);

 if (number %2 == 0)
    printf("%d is even.\n",num);
   else
     printf("%d is odd.\n",num);

     return 0;
     } 
